import LogoImg from '../../logos/logoRNS.png';

const Logo = () => {
  return (
    <div className={'container'}>
      <img alt={'LogoRNStd'} src={LogoImg} className={'logoContainer'} />
    </div>
  );
};

export default Logo;
